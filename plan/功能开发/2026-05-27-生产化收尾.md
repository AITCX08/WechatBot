# 功能开发：生产化收尾

> 日期：2026-05-27 ｜ 类型：功能开发 ｜ 状态：✅ 已完成

## 背景

让 GitHub 仓库 clone 下来能直接用：补齐 README、一键启停脚本、CI、CHANGELOG。

## 改了什么

| 文件 | 内容 |
|------|------|
| `README.MD` | 全中文重写：架构图 / 模块清单 / 多账号配置 / Dashboard API 速查 / Filehelper 命令表 / 故障排查表 |
| `start.bat` | 一键启动：激活 .venv + 首次 pip install + sidecar 单独窗口 + main.py |
| `stop.bat` | 杀 9090 端口 dashboard + sidecar 进程；保留 Weixin 不动 |
| `.github/workflows/test.yml` | windows-latest × Python 3.10/3.11/3.12 跑 pytest |
| `CHANGELOG.md` | 6 个里程碑（迁移/看板/多账号/命令 MVP/v2/生产化），Added/Changed/Fixed/Tests 分节 |

## 为什么这么改

- CI 只装精简测试依赖（跳过 pandas/ipykernel 等重包），跑得快
- CI 跑前先 seed SQLite fixture（`tests/fixtures/sample_contact.py`）
- stop.bat 不杀 Weixin.exe：保留登录态，下次启动免重新扫码

## 怎么验证

- GitHub Actions 首次 run 即通过（commit b0ce2a4，3 个 Python 版本矩阵全绿）
- 191 个测试全绿（含新增的 7 个调度器测试）

## 关联

- commits：`b0ce2a4`（README+脚本+CI）、`735822a`（调度器测试+CHANGELOG）
- 总体规划里程碑表：`plan/开发规划/总体规划.md`
